/*const firstName = 'Julia';
const job = 'developer';
const birthYear = 1990;
const year = 2022;

const julia = "I'm " + firstName + ', a ' + (year - birthYear) +
     ' years old ' + job + '!';
console.log(julia);

const juliaPattern = `I'm + ${firstName}, a ${year - birthYear}  years old ${job}! This is a pattern.`;
*/

const favourite = Number(prompt ("Give me a number"));